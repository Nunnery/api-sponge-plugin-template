dependencies {
    compile project(':notifier-api')
    compile group: 'org.spongepowered', name: 'spongeapi', version: '2.1-SNAPSHOT', changing: false
}

shadowJar {
    dependencies {
        include(project(':notifier-api'))
    }
}